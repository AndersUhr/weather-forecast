import { Layout } from '../components';

export default function Error() {
  return (
    <Layout>
      <h1>
        Unfortunatly we cannot display the weather forecast right now. Try again
        later
      </h1>
    </Layout>
  );
}
